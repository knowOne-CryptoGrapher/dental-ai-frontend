// Legal document version constants.
// Import these into any component that references legal versions — never hardcode inline.
// Bump the version string whenever the corresponding document changes.

export const TERMS_VERSION = "1.2";          // Bumped — AI Technology Disclosure added
export const PRIVACY_POLICY_VERSION = "1.1"; // Bumped — call recording section added
